
import { NextResponse } from "next/server";


export default async function GET() {
  "use server"
  const a="kuldeep";
  // console.log({Mcq});
  const val=JSON.stringify(Mcq);
  // return NextResponse.josn
  
}
